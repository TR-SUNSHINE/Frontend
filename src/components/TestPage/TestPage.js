import MapContainer from "../Map/MapContainer";

const TestPage = () => {
    return (
        <div>
            <header class="header">
                <h2>TEST PAGE</h2>
            </header>

            <MapContainer />
        </div>

    );
};

export default TestPage;