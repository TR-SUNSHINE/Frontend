import RouteMapContainer from "../Map/RouteMapContainer";

const TestPage = () => {
    return (
        <div>
            <header className="header">
                <h2>TEST PAGE</h2>
            </header>

            <RouteMapContainer />
        </div>

    );
};

export default TestPage;